# Space Solution (Platform Scope) — Example Reference

This example demonstrates `write-solution --stage full` at **platform scope**.

The canonical worked example is `docs/solution.md` in this repository. Read
that file directly as the reference; this stub exists so the skill loader can
confirm the examples directory is populated.

Key patterns to observe in `docs/solution.md`:

- §1.1 System context — ASCII diagram of the three-package flow
- §2.1 Quality goals — five goals ordered by priority
- §3 Solution strategy — six named principles each with a consequences list
- §4.2 Level 2 module layout — annotated directory trees in text fences
- §7.4 Rollout control — no application-level flag; worker-routing rationale
- §9 ADR log — candidate ADRs clearly marked "_(Not yet written)_"
- §11 Graduation candidates — table with trigger column

See: `docs/solution.md`
